# Django >= 4.2 is a hard requirement, not a preference: the SSE endpoint uses
# async views with async model querying. On Django < 4.2 that raises
#   TypeError: async_generator object is not iterable
Django>=4.2
daphne>=4.0
